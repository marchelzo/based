class Rectangle {
    init(w, h) {
        self.w = w;
        self.h = h;
    }

    area() {
        return self.w * self.h;
    }
}

class Square : Rectangle {
    init(w) {
        Rectangle.init.bind(self)(w, w);
    }
}

print(Square(5).area());
