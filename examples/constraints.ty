function split(s: String, i: Int = #s / 2) {
    return s.slice(0, i), s.slice(i)
}

// Good.
let left, right = split("Hello, world!")

// Bad. s must be a string!
let left, right = split([1, 2, 3, 4])
